import { colors as _colors, subjectColors as _subjectColors } from './colors';
import { typography as _typography, fontFamily as _fontFamily } from './typography';
import { spacing as _spacing, borderRadius as _borderRadius, radius as _radius, elevation as _elevation, motion as _motion } from './spacing';

export const colors = _colors;
export const subjectColors = _subjectColors;
export const typography = _typography;
export const fontFamily = _fontFamily;
export const spacing = _spacing;
export const borderRadius = _borderRadius;
export const radius = _radius;
export const elevation = _elevation;
export const motion = _motion;

export const theme = {
  colors,
  typography,
  spacing,
  radius,
  elevation,
  motion,
};
