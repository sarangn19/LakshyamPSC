{
  "buildCommand": "npx expo export --platform web",
  "outputDirectory": "dist/client",
  "devCommand": "expo start --web",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
